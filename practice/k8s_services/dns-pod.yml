apiVersion: v1
kind: Pod
metadata:
  name: svc-test-dns
  namespace: service-namespace
spec:
  containers:
    - name: ubuntu
      image: ubuntu
      command:
        - /bin/bash
        - -c
        - |
            apt update && apt install curl -y && sleep 3600
