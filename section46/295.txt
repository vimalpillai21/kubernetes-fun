helm list -A
helm status my-mariadb -n database
copy everything
clear
helm repo update

changes in the mariadb_custom_values.yml config file
change password - Test12345
change username - custom_usr1

cat mariadb_custom_values.yml
helm upgrade -n database --values mariadb_custom_values.yml my-mariadb bitnami/mariadb --version 11.4.0
clear
kubectl get pods -n database
kubectl get pods -n database
kubectl get pods -n database
clear
helm list -A
helm upgrade -n database --values mariadb_custom_values.yml my-mariadb bitnami/mariadb --version 11.3.5
clear
kubectl get pods -n database
kubectl get pods -n database
helm list -A
helm upgrade -n database my-mariadb bitnami/mariadb --version 11.3.5 --> will upgrade and use default values
